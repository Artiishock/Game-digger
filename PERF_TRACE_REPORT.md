# Анализ перф-трейса (Trace-20260627T105721)

> **Источник:** DevTools Performance trace, **десктоп Mac**, `localhost:3000` (dev-сборка),
> эмуляция вьюпорта «Responsive», hostDPR=2. Длина записи **69 секунд**.
>
> ⚠️ Это **dev-сборка на десктопе**, не прод и не iPhone. Цифры по CPU/памяти —
> ориентир, абсолютные значения на реальном устройстве будут другими.
> Где вывод твёрдый, а где гипотеза — помечено явно.

---

## TL;DR

| # | Что | Уверенность |
|---|-----|-------------|
| 1 | **CPU — не бутылочное горлышко.** Поток на 92.5% idle. | ✅ Факт |
| 2 | **Утечка памяти подтверждена**, но в JS-куче ~0.35 МБ/с (не 1). | ✅ Факт |
| 3 | **`resizeCanvas` — разовый фриз ~580 мс на старте (t≈13с)**, не шторм. | ✅ Факт (исправлено) |
| 4 | **Стабильный джанк в геймплее: 30–48% кадров >32 мс**, при простаивающем CPU. | ✅ Факт |
| 5 | Причина джанка из п.4 — **скорее GPU/рендер**, не логика. | ⚠️ Гипотеза |

---

## 1. CPU не грузится — 92.5% idle ✅

CPU-профиль (544k самплов, self-time):
```
92.49%  (idle)
 0.84%  resizeCanvas              (разовый, см. п.3)
 0.30%  updateTransformAndChildren (Pixi traversal)
 0.09%  _debugCountPixiChildren    GameRenderer.ts:1912  ← дебаг, 59ms
 0.06%  LavaSimulation.hit         LavaSimulation.ts:228
 0.05%  Tileworld.update           Tileworld.ts:1027
 0.03%  zustand setState           (60-Гц записи в store)
 0.03%  _tick                      GameRenderer.ts:2738
```
Вся игровая логика — доли процента. **Машина скучает.** Любой нарратив «логика грузит
проц» — неверен.

> ⚠️ На реальном iPhone CPU слабее в разы — то, что на Маке доли процента, там может
> стать заметным. Но *относительная* картина (логика дешёвая, узкое место не в JS) —
> сохранится.

**Действие:** убедиться, что `_debugCountPixiChildren` и прочая дебаг-инструментация
вырезаны из прода (`import.meta.env.DEV`). В трейсе она ела 59 мс — бесплатный налог.

---

## 2. Утечка памяти — подтверждена, ~0.35 МБ/с в JS-куче ✅

```
JS heap: 63.9 MB → 88.3 MB за 69с = +0.35 MB/s
после 150 GC-событий (162 ms) heap НЕ возвращается к базовой линии
nodes DOM: 410 → 549
```
Течь реальная: хип растёт монотонно и не отыгрывается даже после сборок.

> ⚠️ JS-трейс **не видит GPU-память и текстуры**. Осиротевшие Pixi-Graphics из бага
> (`MEMLEAK_FIX.md`) частично живут в GPU — на iPhone суммарный процесс пухнет быстрее
> этих 0.35 МБ/с. Но «горит за 8 минут» — это была переоценка; в JS-куче течь медленная.

**Действие:** фикс из `MEMLEAK_FIX.md` (release визуалов через пул). Бонус: см. п.4 —
осиротевшие объекты ещё и обходятся каждый кадр в `updateTransformAndChildren`.

---

## 3. `resizeCanvas` — разовый фриз на старте, НЕ шторм ✅ (исправление)

Изначально предположил «ресайз дёргается постоянно». **Это неверно.** По таймлайну:
```
весь resizeCanvas (583 ms) сосредоточен в окне t=12.6–13.4с (одно событие)
худшие кадры записи (62–120 мс) — все на t=12.5–13.2с
```
Это **разовая** стоимость: старт игры / построение мира / один ресайз вьюпорта.
Не повторяется по кадрам.

**Но фикс всё равно стоит сделать** — это видимый фриз ~5 кадров при запуске, а на
iPhone он будет крупнее:

[`_applyResize`](src/game/GameRenderer.ts#L4511) делает полный ребилд
(`_buildTunnel`, `_resizeSkyInPlace`, `_syncSurfaceScenery`, `tileWorld.update`) +
**синхронный повторный `renderer.render()`** (строка 4545). Минимально:
```ts
private _applyResize(w, h) {
  if (w === this._lastW && h === this._lastH && /*dpr тот же*/) return  // ← guard
  ...
  // убрать синхронный renderer.render() — ticker отрисует сам в этом же кадре
}
```
И в [`useWindowSize`](src/hooks/useWindowSize.ts) на десктопе слушатель
`visualViewport.scroll` бесполезен и только плодит ре-рендеры App — убрать (нужен
только `resize` + `orientationchange`).

**Приоритет:** средний (разовый хитч, не постоянный).

---

## 4. Стабильный джанк в геймплее — реальный, при простаивающем CPU ✅

Дропы кадров (>32 мс) по 5-сек окнам — НЕ только на старте:
```
 0-  5s : 33%        40- 45s : 39%
 5- 10s : 23%        45- 50s : 48%  ← пик
10- 15s :  1%        55- 60s : 37%
15- 20s : 13%        65- 70s : 40%
```
Окно ресайза (10–15с) как раз чистое по числу дропов — там мало, но огромных кадров.
А вот **активный геймплей (40–70с) рвётся на 30–48%**, и это **не CPU** (он idle) и
**не resize**.

> ⚠️ **Гипотеза (не доказано этим трейсом):** при простаивающем CPU и дропающих кадрах
> узкое место — **GPU/рендер или компоновщик**. Косвенные признаки в трейсе:
> много `Commit` (4518), `SetLayerTreeId` (1871), и в профиле всплывают
> `bindTexture` / `bufferSubData` / `texSubImage2D` (загрузка текстур),
> `collectRenderables` / `updateTransformAndChildren`. Стоимость обхода и отрисовки
> сцены растёт с числом display-объектов — а утечка (п.2) их копит.

**Что проверить, чтобы превратить гипотезу в факт:**
1. **Pixi devtools** (`window.__PIXI_DEVTOOLS__`, [GameRenderer.ts:1311](src/game/GameRenderer.ts#L1311)) —
   число **draw calls** и текстур за кадр. 65 неатлашенных PNG = лишние биндинги/свопы.
2. Chrome DevTools → **трек GPU** (не CPU) на тех же 45–50с — упирается ли в растеризацию.
3. Снять трейс **после фикса утечки** — если джанк в поздних окнах спал, значит его
   подпитывал рост числа объектов.

---

## Итоговые приоритеты (по факту трейса)

1. **Фикс утечки** (`MEMLEAK_FIX.md`) — память **и** стоимость обхода сцены за кадр.
2. **Профиль GPU/draw-calls** — подтвердить причину геймплейного джанка (п.4);
   вероятный следующий шаг — **атлас для 65 PNG** (меньше биндингов текстур).
3. **Guard + убрать синхронный render в `_applyResize`** — убрать стартовый фриз (п.3).
4. **Вырезать дебаг-инструментацию из прода** (`_debugCountPixiChildren` и пр.).
5. Убрать `visualViewport.scroll` из `useWindowSize`; throttle 60-Гц записей в store.

## Чего трейс НЕ подтвердил (мои прошлые переоценки)
- ❌ «Течёт 1 МБ/с, упадёт за 8 минут» → в JS-куче 0.35 МБ/с, медленно.
- ❌ «resizeCanvas дёргается постоянно» → разовый фриз на старте.
- ❌ «CPU грузится логикой» → CPU на 92% idle.
